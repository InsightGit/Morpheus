//
// Created by bobby on 27/11/2020.
//

/*#include "sprite.hpp"

bool morpheus::nds::gfx::Sprite::load_from_pcx(const unsigned char *pcx_data) {
    bool return_value = loadPCX(pcx_data, m_sprite_image.get()) == 0;

    if(return_value) {
        imageTileData(m_sprite_image.get());
    }

    for()

    return return_value;
}*/


